# Playwright CLI workspace (config + session output, regenerated by .devcontainer/setup.sh)
.playwright/

# Local environment overrides (keep the committed template)
.env
.env.*
!.env.example

# Generation skill state (.claude/skills/{audiogen,imagegen2,anim8gen})
# Local caches, prompt history, and run artifacts — regenerated on demand.
.audiogen-history.jsonl
.audiogen-voices.json
.imagegen2-history.jsonl
.anim8gen/
assets/anim8gen/

# Python bytecode (anim8gen runtime tools)
__pycache__/

# Node
node_modules/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
